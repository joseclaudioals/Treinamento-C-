/* Declare e inicialize um array de 5 inteiros. Crie um ponteiro e aponte-o para o início do array. Usando um loop for ou while, percorra todos os elementos do array e imprima seus valores usando apenas o ponteiro. O ponteiro deve ser incrementado a cada iteração para "caminhar" pela memória até o próximo elemento.*/

#include <stdio.h>

int main(void){
    int n = 5;
    int arr[n];
    int* ptr;

    ptr = arr;

    for(int i = 0; i<(lenght(arr)/n); i++){
        
    }
}